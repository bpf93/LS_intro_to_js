/*Given the following code, how can you access the name of 
the person?
*/

let person = {
  name:       'Bob',
  occupation: 'web developer',
  hobbies:    'painting',
};

//via bracket notation:

console.log(person['name']);

//via dot notation:

console.log(person.name);